import { UsPhoneFormatDirective } from './us-phone-format.directive';

describe('UsPhoneFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new UsPhoneFormatDirective();
    expect(directive).toBeTruthy();
  });
});
