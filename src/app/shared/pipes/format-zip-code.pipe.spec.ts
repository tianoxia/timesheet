import { FormatZipCodePipe } from './format-zip-code.pipe';

describe('FormatZipCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatZipCodePipe();
    expect(pipe).toBeTruthy();
  });
});
